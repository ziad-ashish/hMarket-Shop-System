# التصوير ومسح الباركود

## التشغيل

أعد تشغيل البرنامج من `run.bat` لتطبيق الجداول الجديدة عبر `init_db()`.
لا تفتح `index.html` مباشرة عند استخدام المسودات أو حفظ البيانات.
الكاميرا تعمل من النسخة المحلية أو HTTPS؛ فتح عنوان شبكة HTTP من الموبايل لا يضمن إتاحة الكاميرا.

## أماكن الاستخدام

- **نقطة البيع:** مسح بالكاميرا أو من صورة، تأكيد الصنف والكمية، أو إضافة مباشرة اختيارية.
- **إضافة صنف:** تصوير المنتج أو اختيار صورة مع ضغطها قبل الحفظ.
- **الأصناف والمخزون:** مسودة جرد محفوظة لكل مستخدم، عدّ متكرر اختياري، تعديل يدوي، تراجع وتصدير CSV.
- **استلام أمر شراء:** مقارنة المسح بالأصناف المطلوبة ونقل الكميات للمراجعة؛ تغيير المخزون يتطلب تأكيد الاستلام.
- **الفواتير:** فحص وجود الصنف في الفاتورة. الفحص لا ينفذ مرتجعًا.
- **إعدادات الأجهزة:** فتح اختبار التصوير أو قراءة الباركود.

## وحدات الباركود

البحث يبدأ بباركود المحل ثم الشركة ثم الباركود القديم ثم أرقام IMEI/Serial للأجهزة. الباركود الأساسي يمثل وحدة بيع واحدة، ولا يتم افتراض حجم العبوة من شكله.
يمكن ربط **باركود غير مسجل** بصنف ووحدة وعدد صريح من وحدات البيع؛ مثل باركود علبة = 20 قرصًا.
الربط يحتاج صلاحية إدارة الأصناف ولا يستبدل ارتباطًا قائمًا. مسودة الجرد كلها بوحدات البيع؛ تحويل الاستلام لوحدات الشراء يتم بعد التحقق من قابلية القسمة والكمية المتبقية.

## الخصوصية والأداء

- لا تشغيل تلقائي للكاميرا ولا تسجيل فيديو أو إرسال لخدمة تعرف خارجية.
- تتوقف مسارات الكاميرا عند الإغلاق، التنقل، إخفاء الصفحة أو انقطاع الجهاز؛ حتى لو وصل الإذن بعد الإغلاق.
- قراءة مدمجة عند توفرها، وZXing محلي مثبت الإصدار عند الحاجة، بدون CDN أو اعتماد على الإنترنت وقت القراءة.
- وضع اقتصادي يقلل دقة المعالجة ومعدل الفحص. التكبير والإضاءة يظهران فقط عند دعم الجهاز.
- الصور المقبولة JPG/PNG/WebP؛ رفع حتى 12MB قبل الضغط، وحوالي 1MB لكل صورة محفوظة.
- الصور المعتمدة تحفظ بقاعدة SQLite المحلية، والمسودات مرتبطة بالمستخدم. الربط وحفظ المسودات والاستلام مسجل في سجل التدقيق.
- الصور ليست مشفرة داخل قاعدة البيانات؛ يجب حماية ملف القاعدة ونسخه الاحتياطية بصلاحيات Windows المناسبة.

## الاختبار والحدود

`python -m unittest discover -s tests -v`

`node tests/test_camera.cjs`

الاختبارات تستخدم قواعد مؤقتة وتشمل الصلاحيات، المعاملات، تحويل الوحدات، تعارض المسودات، تنظيف الكاميرا وقراءة Code 128 حقيقي بالمكتبة المحلية.
تم اختبار قراءة صورة باركود وتدوير صورة والجرد والتراجع من المتصفح، ومعاينة تنسيق عرض 390px.

يلزم اختبار فعلي على كاميرا جهاز التشغيل للتأكد من الإذن وجودة التركيز والإضاءة والتكبير. لا توجد قراءة تلقائية لخط الطبيب أو استخراج تاريخ الصلاحية من النص. فحص وضوح الصورة إرشادي ولا يضمن وضوح المستند طبيًا.
